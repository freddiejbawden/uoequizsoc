TODO:

Change NavBar so changes alter all pages to make management easier.

Input meta data for all pages

Make changing text easier for those with out git experience

Set up RSS Feed for events?

Fix the Resposive for Kindle Fire HDX (800 x 1280)

Add YAML/JSON/other text thing so people can update
